# Next Actions

作成日: 2026-06-18

## 次に確認すべきこと

1. このリポジトリの位置づけを確認する
   - 提案・設計資料専用リポジトリか
   - 実装リポジトリが別に存在するか
   - 今後このリポジトリに実装を追加する予定があるか

2. Laravel実装の所在を確認する
   - `docs/proposal/08_infrastructure.md` では Laravel + MySQL が想定されています。
   - 実装リポジトリ、検証環境、本番環境、DB定義、デプロイ手順の有無を確認してください。

3. READMEを整備する
   - リポジトリの目的
   - 資料の読み順
   - Markdown/PDF/HTMLの正本ルール
   - 資料生成手順
   - 公開範囲

4. `.DS_Store` の扱いを決める
   - 未管理の `.DS_Store` と `docs/.DS_Store` は削除候補です。
   - `.gitignore` に `.DS_Store` を追加するか確認してください。

5. エクスポート成果物の管理方針を決める
   - `docs/proposal/_proposal__exports/` をGit管理し続けるか
   - 生成物として除外するか
   - 配布成果物として保持する場合、再生成手順とレビュー手順を明記する

6. 依存パッケージの確認
   - `npm audit` は未実行です。
   - パッケージ更新禁止が解除された後、脆弱性と更新影響を確認してください。

7. `.vscode/settings.json` の絶対パスを確認する
   - 個人環境依存のパスが含まれるため、共有設定として妥当か判断してください。

8. 資料内の機密性を確認する
   - 見積、WBS、業務フロー、権限、DB概念、インフラ案は公開範囲を誤るとリスクになります。
   - 社外共有可否、顧客共有可否、内部限定の区分を決めてください。

9. WordPressテーマの依存関係を確認する
   - `wp-content/themes/taguchi_system` の必須プラグインを画面単位で確認してください。
   - `functions.php`、各 `page-*.php`、ACFフィールド利用箇所、ショートコード利用箇所を整理してください。
   - 親テーマ指定は確認できないため、単独テーマとして扱ってよいか確認してください。

10. プラグインの必要性を確定する
   - Advanced Custom Fields、Contact Form 7、WP-Members は必須寄りとして扱います。
   - それぞれ、実際に必要な画面、設定、データ、運用担当を確認してください。
   - 有効化状態はDB未確認のため、WordPress管理画面またはWP-CLIで確認する場合は読み取り専用手順を決めてから実施してください。

11. ECダミー/保留範囲を明確化する
   - WooCommerce本体プラグインは未確認ですが、テーマ内にWooCommerceテンプレートと `WC()->cart` 参照があります。
   - カート、チェックアウト、商品詳細、注文メール、決済、在庫連携は保留範囲として明文化してください。
   - 誤ってECを稼働させないため、検証環境での表示条件・導線・無効化方針を確認してください。

12. 更新禁止範囲を明確化する
   - テーマ、プラグイン、WordPress本体、npm依存は、検証計画なしに更新しないでください。
   - 更新する場合は、バックアップ、差分確認、ロールバック手順、表示確認項目を先に定義してください。

13. MVP仕様を確定する
   - 最新MVPは「FAX/電話発注のオンライン化」です。
   - Webフォーム項目、必須/任意、添付ファイル有無、確認画面有無、送信完了画面を確認してください。
   - メール宛先、CC/BCC、件名、本文、返信先、現場担当者への控え送信有無を確認してください。
   - PDF発注書のレイアウト、発注番号、日付、現場名、商品/資材明細、備考欄、承認欄の要否を確認してください。

14. PDFの扱いを決める
   - PDFをメール添付するか、管理側が確認可能な場所に保存するかを確認してください。
   - 保存する場合は、保存先、ファイル命名規則、保存期間、閲覧権限、再送方法を確認してください。
   - DBによる本格的な注文管理をMVPで必須にしない前提で、最低限の送信履歴をどう残すか確認してください。

15. MVP+1の実施可否を判断する
   - PDF自動保存・自動印刷はMVP必須ではなく、現場運用補助として扱います。
   - 自動保存先、受注用PC名、共有フォルダ、プリンタ名、印刷部数、失敗時通知を確認してください。
   - サーバーから直接プリントする前提にはせず、受注用PCがPDFを取得/同期して印刷する構成を検討してください。
   - 誤印刷、重複印刷、プリンタ停止、同期失敗時の検知・再実行ルールを確認してください。

16. Proposal v1との混同を避ける
   - EC、会員、カート、決済、注文管理、管理画面、在庫、配送、会員管理は現時点のMVP対象外です。
   - 既存proposalは将来構想・拡張ロードマップとして保持し、MVPの見積・WBSとは分けて扱ってください。
   - `docs/proposal/10_estimate.md` の旧MVP概算は、最新MVPの見積として使わないでください。

## 現時点で変更すべきでないもの

今回のAudit目的では、以下は変更しない方針が妥当です。

- 既存の提案資料Markdown
- 既存のPDF/HTMLエクスポート成果物
- `docs/proposal/README.md` 以外の既存proposal本文
- `package.json` / `package-lock.json`
- `.gitignore`
- `.vscode/settings.json`
- `.crossnote/style.less`
- 未管理の `.DS_Store` ファイル
- `wp-content/themes/taguchi_system`
- `wp-content/plugins/advanced-custom-fields`
- `wp-content/plugins/contact-form-7`
- `wp-content/plugins/wp-members`

理由:

- 現時点の目的は現状把握であり、実装・設定変更ではありません。
- 生成物や設定を変更すると、監査対象の現状が変わります。
- `.DS_Store` は削除・ignore候補ですが、今回は「検出した事実」として残してあります。
- WordPressテーマ切替、プラグイン有効化、DB操作、更新は、表示やデータに影響するため今回のAudit範囲外です。

## 次工程の判断材料

次工程は、リポジトリの位置づけによって分岐します。

| 判断 | 次工程 |
|---|---|
| 資料専用リポジトリとして維持 | README整備、資料生成手順整備、公開範囲整理、生成物管理方針決定 |
| 実装をこのリポジトリに追加 | 技術選定確定、Laravel初期構成、環境変数・Docker・CI/CD設計、DB設計詳細化 |
| 実装リポジトリが別にある | 実装リポジトリのAudit、本資料との整合性確認、差分課題化 |
| WordPressとして進める | テーマ依存関係整理、プラグイン有効化計画、EC保留範囲明文化、検証環境での表示確認 |
| 最新MVPとして進める | フォーム項目確定、メール/PDF仕様確定、保存方式決定、MVP+1の自動印刷可否判断 |

## 未確認事項

- 実装リポジトリの有無
- 本番/検証環境の有無
- デプロイ方法
- DBスキーマの実体
- 環境変数の実体と管理方法
- 資料生成コマンド
- パッケージ脆弱性
- エクスポートPDF/HTMLの最新性
- WordPress本体の所在とバージョン
- WordPress DBの状態
- テーマ有効化状態
- プラグイン有効化状態
- ACFフィールド定義の所在
- Contact Form 7フォーム定義の有無
- WP-Members設定の有無
- WooCommerce本体の導入有無と保留範囲
- 現場担当者が入力する発注項目
- 本社・管理側の受信メールアドレス
- PDF発注書のレイアウト
- PDF添付か保存かの運用判断
- 発注控えの送信有無
- FAX/電話を残す例外条件
- MVP+1の受注用PC、共有フォルダ、プリンタ、印刷部数
- 自動印刷失敗時の通知・再印刷手順
