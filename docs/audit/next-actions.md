# Next Actions

作成日: 2026-06-18

## 次に確認すべきこと

1. このリポジトリの位置づけを確認する
   - 提案・設計資料専用リポジトリか
   - 実装リポジトリが別に存在するか
   - 今後このリポジトリに実装を追加する予定があるか

2. Laravel実装の所在を確認する
   - `docs/proposal/08_infrastructure.md` では Laravel + MySQL が想定されています。
   - 実装リポジトリ、検証環境、本番環境、DB定義、デプロイ手順の有無を確認してください。

3. READMEを整備する
   - リポジトリの目的
   - 資料の読み順
   - Markdown/PDF/HTMLの正本ルール
   - 資料生成手順
   - 公開範囲

4. `.DS_Store` の扱いを決める
   - 未管理の `.DS_Store` と `docs/.DS_Store` は削除候補です。
   - `.gitignore` に `.DS_Store` を追加するか確認してください。

5. エクスポート成果物の管理方針を決める
   - `docs/proposal/_proposal__exports/` をGit管理し続けるか
   - 生成物として除外するか
   - 配布成果物として保持する場合、再生成手順とレビュー手順を明記する

6. 依存パッケージの確認
   - `npm audit` は未実行です。
   - パッケージ更新禁止が解除された後、脆弱性と更新影響を確認してください。

7. `.vscode/settings.json` の絶対パスを確認する
   - 個人環境依存のパスが含まれるため、共有設定として妥当か判断してください。

8. 資料内の機密性を確認する
   - 見積、WBS、業務フロー、権限、DB概念、インフラ案は公開範囲を誤るとリスクになります。
   - 社外共有可否、顧客共有可否、内部限定の区分を決めてください。

## 現時点で変更すべきでないもの

今回のAudit目的では、以下は変更しない方針が妥当です。

- 既存の提案資料Markdown
- 既存のPDF/HTMLエクスポート成果物
- `package.json` / `package-lock.json`
- `.gitignore`
- `.vscode/settings.json`
- `.crossnote/style.less`
- 未管理の `.DS_Store` ファイル

理由:

- 現時点の目的は現状把握であり、実装・設定変更ではありません。
- 生成物や設定を変更すると、監査対象の現状が変わります。
- `.DS_Store` は削除・ignore候補ですが、今回は「検出した事実」として残してあります。

## 次工程の判断材料

次工程は、リポジトリの位置づけによって分岐します。

| 判断 | 次工程 |
|---|---|
| 資料専用リポジトリとして維持 | README整備、資料生成手順整備、公開範囲整理、生成物管理方針決定 |
| 実装をこのリポジトリに追加 | 技術選定確定、Laravel初期構成、環境変数・Docker・CI/CD設計、DB設計詳細化 |
| 実装リポジトリが別にある | 実装リポジトリのAudit、本資料との整合性確認、差分課題化 |

## 未確認事項

- 実装リポジトリの有無
- 本番/検証環境の有無
- デプロイ方法
- DBスキーマの実体
- 環境変数の実体と管理方法
- 資料生成コマンド
- パッケージ脆弱性
- エクスポートPDF/HTMLの最新性
